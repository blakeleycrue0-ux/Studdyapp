# Copia este archivo como .env para desarrollo local con `netlify dev`.
# En producción, estas mismas variables se configuran en:
# Netlify → Site configuration → Environment variables
#
# El archivo .env real está en .gitignore y NUNCA debe subirse al repositorio.

SUPABASE_URL=
SUPABASE_ANON_KEY=
ANTHROPIC_API_KEY=
